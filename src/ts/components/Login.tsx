import * as React from "react";

interface IProps {

}

export const Login = (props: IProps) => {
    return (
        <>
            <form>
                <input/>
                <input/>
            </form>

            <button>
                Wyślij
            </button>
        </>
    );
};
